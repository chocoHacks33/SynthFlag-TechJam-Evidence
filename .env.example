# Server-side only. Rotate any credential previously pasted into a chat.
NVIDIA_API_KEY=
NVIDIA_MODEL=nvidia/nemotron-3.5-lightning-30b-a3b

# Optional FastAPI inference worker exposing /health, /v1/analyze, /v1/analyze-frames.
MODEL_API_URL=
